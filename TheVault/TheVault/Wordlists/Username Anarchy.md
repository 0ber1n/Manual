Used to create a username list of different combinations for a name. Must install Ruby

```CLI
sudo apt install ruby -y
```

```CLI
git clone https://github.com/urbanadventurer/username-anarchy.git
```
